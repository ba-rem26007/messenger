{% extends 'base.html.twig' %}

{% block title %}Editer le Message{% endblock %}

{% block body %}
    <h1>Editer le Message</h1>

    {{ include('message/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('message_index') }}">{{ "retourner à la liste des messages"|button("secondary") }}</a>

    {{ include('message/_delete_form.html.twig') }}
    
{% endblock %}
