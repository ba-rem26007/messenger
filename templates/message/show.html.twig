{% extends 'base.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ message.title }}</td>
            </tr>
            <tr>
                <th>Canal</th>
                <td>{{ message.choice }}</td>
            </tr>
            <tr>
                <th>Date d'emission</th>
                <td>{{ message.EmissionDate ? message.EmissionDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('message_index') }}">{{ 'Retour à la liste'|button('secondary') }}</a>
    <br>
    <a href="{{ path('message_edit', {'id': message.id}) }}">{{ 'edit'|button('warning') }}</a>
    <br>
    {{ include('message/_delete_form.html.twig') }}
{% endblock %}
